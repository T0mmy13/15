<Page x:Class="EFCORE15.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EFCORE15.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="800"
      Background="{StaticResource MaterialDesign.Brush.Background}"
      >

    <Grid>
        <materialDesign:Card Width="350" Padding="25" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Вход в систему" Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <StackPanel x:Name="RoleSelectionPanel" Visibility="Visible">

                    <Button Click="UserLogin" Content="Посетитель" Margin="0,5"/>
                </StackPanel>

                <StackPanel x:Name="PinCodePanel">
                    <TextBlock Text="Введите PIN-код" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                    <PasswordBox x:Name="PinCodeField" MaxLength="4"
                                 Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                 HorizontalContentAlignment="Center"
                                 Margin="0,0,0,20"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Click="BackToRoles" Content="Отмена" Margin="0,0,5,0"/>
                        <Button Grid.Column="1" Click="CheckPinAndLogin" Content="Войти" Margin="5,0,0,0"/>
                    </Grid>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Page>