<Page x:Class="EFCORE15.Pages.AddProductPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EFCORE15.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:validator="clr-namespace:EFCORE15.Validators"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Loaded="LoadList"
      Background="{DynamicResource MaterialDesignPaper}"
      Title="Добавление товара">

    <Page.Resources>
        <validator:NameValidator x:Key="nameValidator"/>
        <validator:PriceValidator x:Key="priceValidator"/>
        <validator:StockValidator x:Key="stockValidator"/>
        <validator:RatingValidator x:Key="ratingValidator"/>
        <validator:DescriptionValidator x:Key="descriptionValidator"/>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Добавление товара" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,20"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBox x:Name="Name" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Название товара" Margin="0,0,0,15">
                        <TextBox.Text>
                            <Binding Path="Product.Name" UpdateSourceTrigger="PropertyChanged" NotifyOnValidationError="True">
                                <Binding.ValidationRules>
                                    <validator:NameValidator ValidatesOnTargetUpdated="True"/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ComboBox x:Name="CategoryComboBox" Grid.Column="0"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                  materialDesign:HintAssist.Hint="Категория"
                                  ItemsSource="{Binding Categories}" 
                                  DisplayMemberPath="Name" 
                                  SelectedValuePath="Id"
                                  SelectedValue="{Binding Product.CategoryId, UpdateSourceTrigger=PropertyChanged}">
                        </ComboBox>

                        <ComboBox x:Name="BrandComboBox" Grid.Column="2"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                  materialDesign:HintAssist.Hint="Бренд"
                                  ItemsSource="{Binding Brands}" 
                                  DisplayMemberPath="Name" 
                                  SelectedValuePath="Id"
                                  SelectedValue="{Binding Product.BrandId, UpdateSourceTrigger=PropertyChanged}">
                        </ComboBox>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="Price" Grid.Column="0" Style="{StaticResource MaterialDesignOutlinedTextBox}" materialDesign:HintAssist.Hint="Цена">
                            <TextBox.Text>
                                <Binding Path="Product.Price" UpdateSourceTrigger="LostFocus" NotifyOnValidationError="True" StringFormat="F2">
                                    <Binding.ValidationRules>
                                        <validator:PriceValidator ValidatesOnTargetUpdated="True"/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>

                        <TextBox x:Name="Stock" Grid.Column="2" Style="{StaticResource MaterialDesignOutlinedTextBox}" materialDesign:HintAssist.Hint="Склад">
                            <TextBox.Text>
                                <Binding Path="Product.Stock" UpdateSourceTrigger="PropertyChanged" NotifyOnValidationError="True">
                                    <Binding.ValidationRules>
                                        <validator:StockValidator ValidatesOnTargetUpdated="True"/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>

                        <TextBox x:Name="Rating" Grid.Column="4" Style="{StaticResource MaterialDesignOutlinedTextBox}" materialDesign:HintAssist.Hint="Рейтинг">
                            <TextBox.Text>
                                <Binding Path="Product.Rating" UpdateSourceTrigger="LostFocus" NotifyOnValidationError="True" StringFormat="F1">
                                    <Binding.ValidationRules>
                                        <validator:RatingValidator ValidatesOnTargetUpdated="True"/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBox x:Name="Description" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Описание"
                             Height="120" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                             AcceptsReturn="True" Margin="0,0,0,15">
                        <TextBox.Text>
                            <Binding Path="Product.Description" UpdateSourceTrigger="PropertyChanged" NotifyOnValidationError="True">
                                <Binding.ValidationRules>
                                    <validator:DescriptionValidator ValidatesOnTargetUpdated="True"/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>

                    <TextBlock Text="Теги:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    
                    <Grid Height="200">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="Доступные" Grid.Column="0" Padding="0">
                            <ListBox ItemsSource="{Binding Tags}" 
                                     SelectedItem="{Binding SelectedTag}" 
                                     DisplayMemberPath="Name" 
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                        </GroupBox>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5">
                            <Button Content=">" Click="AddTag" ToolTip="Добавить тег" Margin="0,0,0,10" Width="30" Padding="0"/>
                            <Button Content="&lt;" Click="RemoveTag" ToolTip="Убрать тег" Width="30" Padding="0"/>
                        </StackPanel>

                        <GroupBox Header="Выбранные" Grid.Column="2" Padding="0">
                            <ListBox ItemsSource="{Binding ProductTags}" 
                                     SelectedItem="{Binding SelectedProductTag}" 
                                     DisplayMemberPath="Name" 
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                        </GroupBox>
                    </Grid>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Click="Back" Content="Отмена" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0"/>
                <Button Click="AddProduct" Content="Добавить товар" Width="150"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>