<Page x:Class="EFCORE15.Pages.AddPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EFCORE15.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Background="{DynamicResource MaterialDesignPaper}"
      Loaded="LoadList">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20">
            <TextBlock Text="Справочники и управление" 
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                       VerticalAlignment="Center"/>
            <Button Click="GoAddProductPage" Content="+ Добавить новый товар" 
                    HorizontalAlignment="Right" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </Grid>

        <Grid Grid.Row="1" Margin="10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" Margin="10" Padding="10" >
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Категории" FontWeight="Bold" Margin="0,0,0,10" Foreground="Gray"/>

                    <ListBox x:Name="CategoriesList" Grid.Row="1"
                             ItemsSource="{Binding categories}"
                             SelectedItem="{Binding category}"
                             MouseDoubleClick="CategoriesList_MouseDoubleClick"
                             DisplayMemberPath="Name"/>

                    <Button Grid.Row="2" Click="GoAddCategoryPage" Content="Добавить категорию" 
                            Margin="0,10,0,0" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                </Grid>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1" Margin="10" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Бренды" FontWeight="Bold" Margin="0,0,0,10" Foreground="Gray"/>

                    <ListBox x:Name="BrandsList" Grid.Row="1"
                             ItemsSource="{Binding brands}"
                             SelectedItem="{Binding brand}"
                             MouseDoubleClick="BrandsList_MouseDoubleClick"
                             DisplayMemberPath="Name"/>

                    <Button Grid.Row="2" Click="GoAddBrandPage" Content="Добавить бренд" 
                            Margin="0,10,0,0" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                </Grid>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="2" Margin="10" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Теги" FontWeight="Bold" Margin="0,0,0,10" Foreground="Gray"/>

                    <ListBox x:Name="TagsList" Grid.Row="1"
                             ItemsSource="{Binding tags}"
                             SelectedItem="{Binding tag}"
                             MouseDoubleClick="TagsList_MouseDoubleClick"
                             DisplayMemberPath="Name"/>

                    <Button Grid.Row="2" Click="GoAddTagPage" Content="Добавить тег" 
                            Margin="0,10,0,0" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                </Grid>
            </materialDesign:Card>
        </Grid>

        <Button Grid.Row="2" Click="Back" Content="Назад в главное меню" 
                Style="{StaticResource MaterialDesignFlatButton}" 
                HorizontalAlignment="Left" Margin="20"/>
    </Grid>
</Page>