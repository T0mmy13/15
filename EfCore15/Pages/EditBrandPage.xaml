<Page x:Class="EFCORE15.Pages.EditBrandPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EFCORE15.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:validator="clr-namespace:EFCORE15.Validators"
      mc:Ignorable="d" 
      d:DesignHeight="350" d:DesignWidth="500"
      Background="{DynamicResource MaterialDesignPaper}">

    <Page.Resources>
        <validator:BrandValidator x:Key="brandCategoryTagValidator"/>
    </Page.Resources>

    <Grid>
        <materialDesign:Card Margin="0" Padding="20" VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
            <StackPanel>
                <TextBlock Text="Редактирование бренда" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,20"/>

                <TextBox x:Name="BrandNameInput" Style="{StaticResource MaterialDesignOutlinedTextBox}" 
                         materialDesign:HintAssist.Hint="Название бренда" Margin="0,0,0,5">
                    <TextBox.Text>
                        <Binding Path="Name" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True" NotifyOnValidationError="True">
                            <Binding.ValidationRules>
                                <validator:BrandValidator/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>

                <ItemsControl ItemsSource="{Binding ElementName=BrandNameInput, Path=(Validation.Errors)}" Margin="0,0,0,20">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ErrorContent}" Foreground="Red" FontSize="12"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>


                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Click="Delete" HorizontalAlignment="Left" Content="Удалить" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="Red" BorderBrush="Red" Padding="10,5"/>
                        <Button Click="Back" Content="Отмена" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0"/>
                        <Button Click="Edit" Content="Сохранить"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Page>